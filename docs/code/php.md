# PHP

## 进制转换

```php
// 十六进制 十进制
var_dump(hexdec('abc'));
var_dump(dechex(2748));

// 二进制 十进制
var_dump(bindec('110011'));
var_dump(decbin(51));

// 八进制 十进制
var_dump(octdec('77'));
var_dump(decoct(63));
```